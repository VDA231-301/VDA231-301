# Example Generator Test Prompts

The following prompts can be used to test the Example Generator Agent.

## Material Modelling

Create an example for a simple PP-GF30 material.

Create an example for a material with multiple approved sources.

Create an example for a layered interior trim material.

## Component Modelling

Create an example for a component instance including production batch, cavity and production date.

Create an example showing NGID-based identification of a component.

## Specification Modelling

Create an example demonstrating an OEM-specific odor requirement deviation based on VDA 270.

Create an example demonstrating a color-dependent requirement.

Create an example demonstrating a layer-specific requirement.

## Testing Modelling

Create an example for a VDA 270 odor test.

Create an example for a VDA 278 emission test.

Create a complete minimal TestingProject example.

## Future Modelling

Create an example for a future schema extension.

Create an example for a specification hierarchy.

Create an example based on a user-defined business scenario.
